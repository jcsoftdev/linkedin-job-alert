{
  "name": "linkedin-alert-job",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && bun i && bun run build",
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "dev:client": "cd client && bun run dev",
    "dev:debug": "bun --watch --inspect src/index.ts",
    "check": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "LinkedIn Alert Hub collects, filters, and streams relevant LinkedIn job posts so you can act on the best opportunities faster.",
  "dependencies": {
    "hono": "^4.11.7",
    "node-cron": "^4.2.1",
    "openai": "^6.17.0",
    "puppeteer": "^24.36.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.8",
    "@types/node": "^25.2.0",
    "@types/node-cron": "^3.0.11",
    "typescript": "^5.9.3"
  }
}
